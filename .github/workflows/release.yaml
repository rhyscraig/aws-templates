name: Release

on:
  push:
    branches: [ main ]
    paths:
      - '**.tf'
      - '**.tfvars'
      - '.github/release.yaml' # Re-trigger if config changes

permissions:
  contents: write

jobs:
  release:
    # Point to the shared workflow
    uses: rhyscraig/aws-terraform-platform-aws-workflows/.github/workflows/reusable-tagging.yaml@v2.0.0
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}